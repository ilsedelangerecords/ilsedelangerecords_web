name: 🚀 Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force deployment even if no changes detected'
        required: false
        default: false
        type: boolean

concurrency:
  group: production-deployment
  cancel-in-progress: false

jobs:
  # Pre-deployment checks
  pre-deploy-checks:
    runs-on: ubuntu-latest
    outputs:
      should-deploy: ${{ steps.changes.outputs.should-deploy }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history to ensure all files are present

      - name: 🔍 Check for relevant changes
        id: changes
        run: |
          if [[ "${{ github.event.inputs.force_deploy }}" == "true" ]]; then
            echo "should-deploy=true" >> $GITHUB_OUTPUT
            echo "🔄 Force deployment requested"
          else
            echo "should-deploy=true" >> $GITHUB_OUTPUT
            echo "✅ Deploying to ensure staging is up to date"
          fi

      - name: 📋 Generate version
        id: version
        run: |
          VERSION=$(date +%Y%m%d-%H%M%S)-$(git rev-parse --short HEAD)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 Version: $VERSION"

  # Build and test
  build:
    needs: pre-deploy-checks
    if: needs.pre-deploy-checks.outputs.should-deploy == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history to ensure all files are present

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: 📥 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🏗️ Build application
        run: pnpm run build

      - name: 🔍 List dist directory contents
        run: ls -R dist/

      - name: ✅ Validate content files exist
        run: |
          echo "🔍 Checking content files..."
          if [ -f "dist/content/albums.json" ]; then
            ALBUM_COUNT=$(cat dist/content/albums.json | jq 'length // (. | keys | length)')
            echo "📀 Albums found: $ALBUM_COUNT"
          else
            echo "❌ albums.json not found in dist/content"
            exit 1
          fi
          if [ -f "dist/content/lyrics.json" ]; then
            LYRICS_COUNT=$(cat dist/content/lyrics.json | jq 'length // (. | keys | length)')
            echo "🎵 Lyrics found: $LYRICS_COUNT"
          else
            echo "❌ lyrics.json not found in dist/content"
            exit 1
          fi
          if [ -f "dist/content/artists.json" ]; then
            ARTISTS_COUNT=$(cat dist/content/artists.json | jq 'length // (. | keys | length)')
            echo "👤 Artists found: $ARTISTS_COUNT"
          else
            echo "❌ artists.json not found in dist/content"
            exit 1
          fi

      - name: ✅ Verify build output
        run: |
          echo "📦 Build completed successfully!"
          echo "📁 Build size:"
          du -sh dist/
          echo "📄 Files generated:"
          find dist/ -type f | wc -l
          echo "🔍 Checking critical files:"
          ls -la dist/index.html
          ls -la dist/content/ || echo "⚠️ No content directory in build"
          ls -la dist/images/ || echo "⚠️ No images directory in build"
          ls -la dist/favicon.ico || echo "⚠️ No favicon in build"

      - name: 📤 Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ needs.pre-deploy-checks.outputs.version }}
          path: dist/
          retention-days: 30

  # Deploy to staging
  deploy:
    needs: [pre-deploy-checks, build]
    if: needs.pre-deploy-checks.outputs.should-deploy == 'true'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history to ensure all files are present

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: 📥 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🏗️ Build for production
        run: pnpm run build

      - name: 🔐 Prepare SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p ${{ secrets.SSH_PORT || '22' }} ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: 🚀 Deploy via rsync
        run: |
          echo "🚀 Deploying to staging server..."
          echo "📊 Pre-deployment verification:"
          ls -la dist/
          echo "📁 Content files:"
          ls -la dist/content/ || echo "No content directory"
          echo "🖼️ Image files:"
          ls -la dist/images/ | head -5 || echo "No images directory"
          
          echo "🔄 Starting rsync deployment..."
          rsync -avz --delete --progress \
            -e "ssh -i ~/.ssh/id_ed25519 -p ${{ secrets.SSH_PORT || '22' }}" \
            dist/ \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.SSH_TARGET_PATH }}
          echo "✅ Deployment completed!"

      - name: 🌐 Deployment summary
        run: |
          echo "🎉 Deployment successful!"
          echo "🔗 Staging URL: https://staging.ilsedelangerecords.nl/"
          echo "📦 Version: ${{ needs.pre-deploy-checks.outputs.version }}"
          echo "⏰ Deployed at: $(date)"

      - name: 🧪 Post-deployment verification
        run: |
          echo "🔍 Verifying deployment..."
          sleep 15
          echo "🌐 Testing staging site..."
          if curl -f -s https://staging.ilsedelangerecords.nl/ > /dev/null; then
            echo "✅ Staging site is responding"
          else
            echo "⚠️ Staging site may not be ready yet"
          fi
          
          echo "📊 Testing content files..."
          if curl -f -s https://staging.ilsedelangerecords.nl/content/albums.json > /dev/null; then
            echo "✅ Albums content is accessible"
          else
            echo "⚠️ Albums content not accessible"
          fi


